% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{verdict_metrics}
\alias{verdict_metrics}
\alias{precision}
\alias{recall}
\alias{f1_meas}
\title{Precision, recall, and F1-score with class exclusion}
\usage{
precision(
  data,
  truth,
  estimate,
  estimator = "macro",
  classes_exclude = NULL,
  ...
)

recall(data, truth, estimate, estimator = "macro", classes_exclude = NULL, ...)

f1_meas(
  data,
  truth,
  estimate,
  estimator = "macro",
  classes_exclude = NULL,
  ...
)
}
\arguments{
\item{data}{A data frame containing columns for truth and predictions.}

\item{truth}{The column name (unquoted) of the true class labels (factor).}

\item{estimate}{The column name (unquoted) of the predicted class labels
(factor with the same levels as \code{truth}).}

\item{estimator}{One of \code{"macro"} (default) or \code{"macro_weighted"}. Matches
the corresponding \code{estimator} values in yardstick: \code{"macro"} averages
per-class scores unweighted, \code{"macro_weighted"} weights by support.}

\item{classes_exclude}{Character vector of class names to exclude from the
average. Excluded classes do not contribute to the metric. Default
\code{NULL} includes all classes.}

\item{...}{Currently unused. Reserved for future arguments.}
}
\value{
A tibble with one row and columns \code{.metric}, \code{.estimator},
\code{.estimate}, matching the yardstick metric output format.
}
\description{
Single-value precision, recall, and F1-score, matching the yardstick
output format (\code{.metric}, \code{.estimator}, \code{.estimate}), with support for
excluding specific classes from the average — the same \code{classes_exclude}
logic as \code{\link[=classification_report]{classification_report()}}.
}
\examples{
library(tibble)
truth <- factor(c("cat", "cat", "dog", "dog", "fish", "fish"),
                levels = c("cat", "dog", "fish"))
pred  <- factor(c("cat", "dog", "dog", "dog", "fish", "cat"),
                levels = c("cat", "dog", "fish"))
df <- tibble(truth = truth, pred = pred)

# Default: unweighted mean across classes ("macro")
precision(df, truth, pred)

# Weight each class's score by its support instead
precision(df, truth, pred, estimator = "macro_weighted")

# Drop "fish" from the average entirely (still "macro" by default)
precision(df, truth, pred, classes_exclude = "fish")

# Combine both: weighted average over the remaining classes
precision(df, truth, pred, estimator = "macro_weighted", classes_exclude = "fish")

# recall() and f1_meas() take the same arguments
recall(df, truth, pred, estimator = "macro_weighted")
f1_meas(df, truth, pred, classes_exclude = "fish")

}
